# שימוש ב-Node.js כתמונה בסיסית
FROM node:18

# הגדרת תיקיית העבודה בתוך הקונטיינר
WORKDIR /app

# העתקת קבצי הפרויקט לתוך הקונטיינר
COPY . .

# התקנת חבילות npm
RUN npm install -g serve

# הגדרת הפורט שבו האתר ירוץ
EXPOSE 3000

# פקודת ההרצה
CMD ["serve", "-s", ".", "-l", "3000"]
